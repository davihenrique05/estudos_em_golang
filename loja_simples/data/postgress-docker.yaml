services:
  
    db:
      image: postgres:latest
      restart: always
      environment:
        POSTGRES_USER: root
        POSTGRES_PASSWORD: root
        POSTGRES_HOST: postgres
        POSTGRES_DB: loja_simples
      volumes: 
        - ./:/docker-entrypoint-initdb.d